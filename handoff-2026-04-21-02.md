# Handoff: Academic Clarity - DeepSeek OCR 修复与验证版 (2026-04-21)

## 1. 项目概述
- **项目名称**: Academic Clarity (AI Researcher)
- **类型**: 桌面级学术文献管理与 AI 增强阅读器。
- **技术栈**:
    - **前端**: React 19 + Vite (Port: 30517) + Framer Motion + react-pdf。
    - **后端**: Python 3 + FastAPI (Port: 38392) + SQLite (WAL 模式)。
    - **核心引擎**: 10-Worker 并行任务中枢 (`asyncio.Queue`)。
- **前端包管理器**: pnpm (v10+)。
- **前端目录**: `src/` (组件化架构)。
- **一句话说明**: 这是一个支持 10 路 AI 并发处理（OCR + 提取）的学术智库，具备全维度手动拖拽布局和结果导向的自动同步能力。

## 2. 本次会话完成的工作
- **目标**: 修复 DeepSeek OCR 无法正确输出 Markdown 格式和 DOI 提取失败的问题，确保完整的学术文献处理流程。

### Git 节点记录
| Hash | 描述 | 关键改动 |
| :--- | :--- | :--- |
| `d6c9de3` | fix: resolve DeepSeek OCR markdown format and DOI extraction issues | 修复 `clean_ocr_markdown` 正则表达式，保留 ref 标签内容，确保 Markdown 格式和 DOI 提取正常。 |
| `b9f616d` | feat: stabilize industrial OCR... | 锁定 4000 Token 限制，引入 Blob 加载 PDF，对齐跨栈测试。 |
| `e5e63eb` | feat: merge high-fidelity OCR cleanup... | 整合官方 OCR 清洗逻辑，彻底剔除布局标签保护正文。 |
| `f7d876e` | fix: resolve PDF loading failure... | 通过 Blob URL 解决 PDF 加载失败，增强 DOI 提取引导。 |
| `e8f0129` | test: complement UI interaction tests... | 补全筛选、拖拽及后端 JOIN 的 100% 行为测试。 |

### 具体改动清单
1.  **OCR 清洗逻辑修复 (Clean OCR Markdown)**
    - **文件**: `backend/utils/text_processor.py`
    - **改动**: 修改 `clean_ocr_markdown` 函数的正则表达式，从 `re.sub(pattern, '', md_text)` 改为 `re.sub(pattern, r'\1', md_text)`，保留 `<|ref|>` 标签内的文字内容。
    - **决策**: 解决了 DeepSeek-OCR 输出格式 `<|ref|>内容<|/ref|><|det|>坐标<|/det|>` 中内容被误删的问题，确保 Markdown 格式和关键信息不丢失。
    - **关键代码**:
      ```python
      # 修复前
      pattern = r'<\|ref\|>.*?<\|/ref\|><\|det\|>.*?<\|/det\|>'
      cleaned = re.sub(pattern, '', md_text)  # 错误：连内容一起删了
      
      # 修复后
      pattern = r'<\|ref\|>(.*?)<\|/ref\|><\|det\|>.*?<\|/det\|>'
      cleaned = re.sub(pattern, r'\1', md_text)  # 正确：只删坐标，保留内容
      ```

2.  **单元测试更新 (Test Text Processing)**
    - **文件**: `tests/unit/test_text_processing.py`
    - **改动**: 更新 `test_clean_ocr_markdown_removes_tags` 测试用例，从期望空字符串改为期望保留 ref 标签内的内容。
    - **决策**: 确保测试与新的清洗逻辑保持一致，验证修复效果。
    - **关键代码**:
      ```python
      # 修复前
      raw = "<|ref|>image<|/ref|><|det|>1,2,3,4<|/det|> # Header"
      self.assertEqual(cleaned, "# Header")
      
      # 修复后
      raw = "<|ref|>Title Text<|/ref|><|det|>1,2,3,4<|/det|> # Header"
      self.assertEqual(cleaned, "Title Text # Header")
      ```

3.  **端到端验证 (OCR Workflow Test)**
    - **文件**: `tests/ocr_lab/` (临时测试文件)
    - **改动**: 创建多个测试脚本验证完整 OCR 工作流，包括 PDF 转图片、OCR 识别、Markdown 清洗、DOI 提取和 Basic Insight 生成。
    - **决策**: 确保修复后的系统能正确处理真实学术 PDF，验证 Markdown 格式和 DOI 提取功能。

### 已修改文件列表
| 文件路径 | 改动摘要 |
| :--- | :--- |
| `backend/utils/text_processor.py` | **核心修复**: 修复 `clean_ocr_markdown` 正则表达式，保留 ref 标签内的文字内容。 |
| `backend/services/ai_service.py` | **Prompt 优化**: 保持官方推荐的 OCR prompt 格式，确保 `<|grounding|>` token 被正确识别。 |
| `tests/unit/test_text_processing.py` | **测试更新**: 更新测试用例以匹配新的清洗逻辑，确保测试通过。 |

## 3. 尚未完成的工作
- **高**: **OCR Markdown 在线编辑器**。目前 Markdown 仅供查看，用户发现错别字无法手动校正（优先级：极高）。
- **中**: **批量操作界面**。目前只能一键同步全库，缺乏“选中多项删除/重新处理”的 UI（优先级：中）。
- **低**: **全局元数据导出**。需要一键生成整个工作区的 Excel 或 BibTeX 报告（优先级：低）。

## 4. 设计规范参考
- **颜色系统**:
    - 🔴 `#f43f5e` (Rose-500) -> 任务失败或无原文。
    - 🟡 `#f59e0b` (Amber-500) -> 仅有原文，缺 AI 洞察。
    - 🟢 `#10b981` (Emerald-500) -> 情报闭环，全绿。
- **拖拽手柄**: 1.5px 宽的 `slate-200` 线条，Hover 时变为 `indigo-400` 并显示 `GripVertical` 图标。

## 5. 操作指引
- **如何继续**:
    1.  运行 `.
un_tests.bat`。必须看到 12 个任务全绿。
    2.  如果需要添加新功能，先在 `src/types/index.ts` 定义数据模型。
    3.  任何 UI 按钮修改必须更新对应的 `data-testid`。
- **如何回滚**: `git reset --hard d6c9de3` (此节点为本次修复的稳定版本)。
- **项目启动**: `.
un_app.bat`。
- **注意事项**:
    - **本地 Key**: 修改 `backend/config.py`，不要动 `config.py.example`。
    - **测试**: 严禁绕过 `run_tests.bat` 直接 Commit。
    - **依赖**: 使用 pnpm 管理前端包，不要混用 npm 或 yarn。
