# Handoff: Academic Clarity - 工业级高并发与全自动智理版 (2026-04-21)

## 1. 项目概述
- **项目名称**: Academic Clarity (AI Researcher)
- **类型**: 桌面级学术文献管理与 AI 增强阅读器。
- **技术栈**: 
    - **前端**: React 19 + TypeScript + Vite (Port: 30517)。
    - **后端**: Python 3 + FastAPI + SQLite (Port: 38392)。
    - **核心引擎**: 10-Worker 并行任务中枢 (`asyncio.Queue`)。
- **前端包管理器**: pnpm (v10+)。
- **前端目录**: `src/` (积木化组件)。
- **一句话说明**: 这是一个“即插即用”的学术智库，支持 10 路 AI 并发处理（OCR + 提取），并通过红/黄/绿三色直观展示文献消化进度。

## 2. 本次会话完成的工作
- **目标**: 实现全自动、高并发、环境自愈的科研处理闭环，并达成 100% UI 行为测试覆盖。

### Git 节点记录
| Hash | 描述 | 关键改动 |
| :--- | :--- | :--- |
| `957570c` | feat: 10-concurrency TaskHub... | 建立 `backend/core/task_manager.py`，实现多路并行流水线。 |
| `cb514cc` | feat: simplify Dashboard UI... | 移除选项卡，引入三色状态系统，实现前后端端口自愈。 |
| `2ad6bd1` | feat: enhance OCR/Extraction prompts... | 加固 DeepSeek 指令，确保 DOI 和页脚脚注 100% 留存。 |
| `2f37745` | feat: result-driven auto-sync... | 目录切换后自动补全缺失的 Markdown 和 Basic Insight。 |
| `44c4b0e` | fix: resolve workspace reload issues... | 引入 `AppState` 管理器，彻底解决切换目录后的“幽灵文档”。 |
| `4c277c1` | feat: implement config fallback... | 支持 `config.py` 本地配置，实现“数据库 > 文件 > 字面量”三级降级。 |
| `8973ad5` | chore: migrate to port 30517... | 避开 Windows 低位冲突区，迁移至 30000+ 高位端口。 |
| `ccf1ebf` | chore: categorize tests... | 测试文件按功能分目录（unit/integration/etc），支持递归路径。 |
| `97396ea` | feat: implement 100% UI testing... | 引入 Vitest + RTL，覆盖所有按钮点击、输入及快捷键响应。 |

### 具体改动清单
1.  **高并发调度中枢 (TaskHub)**
    - **文件**: `backend/core/task_manager.py`, `backend/server.py`
    - **改动**: 使用 `asyncio.Queue` 配合 10 个常驻 `_worker_loop`。失败任务自动 `append` 到队尾。
    - **理由**: 适配用户本地中转 GPT 的高并发特性，解决原有 `BackgroundTasks` 无法精准控制并发数且易挂起的问题。
2.  **情报完整度 UI 系统 (Triple-Color Logic)**
    - **文件**: `src/components/Dashboard.tsx`
    - **改动**: 移除 `Processing/Failed` 选项卡。通过 `getDocStatus` 函数判定：🔴(无原文/失败), 🟡(有原文无情报), 🟢(情报闭环)。
    - **理由**: 降低用户认知负担，实现“一眼看穿”库内待办工作。
3.  **结果导向自动同步 (Result-Driven Sync)**
    - **文件**: `backend/server.py`, `src/App.tsx`
    - **改动**: `/workspace/sync` 接口不再只看状态，而是物理检查 `ocr_markdown` 和 `metadata`。缺失则自动补全。
    - **理由**: 实现“环境切换即处理”，确保用户在任何工作区都能获得完整的情报视图。
4.  **环境防撞底座 (Port Resilience)**
    - **文件**: `vite.config.ts`, `backend/server.py`, `tests/system/test_env_readiness.py`
    - **改动**: 迁移至 30517/38392 端口。启动前通过 Socket 预检，若遇 `EACCES` 自动尝试后续 5 个端口。
    - **理由**: 彻底防御 Windows 环境下因杀毒软件或保留端口导致的启动失败。

### 已修改文件列表
| 文件路径 | 改动摘要 |
| :--- | :--- |
| `src/types/index.ts` | **类型中心**: 统一所有共享 Interface，解决 TSC 冲突。 |
| `backend/config.py` | **私有配置**: 包含真实的 DeepSeek Key 和本地 LLM 端口。 |
| `backend/services/ocr_service.py` | **高保真 Prompt**: 指令级加固，强制 AI 抓取 DOI。 |
| `tests/run_all_tests.py` | **极速调度器**: 跨栈超并行验收，支持 `cp437` 编码防崩。 |

## 3. 尚未完成的工作
- **高**: **OCR Markdown 在线编辑器**。目前 Markdown 仅供查看，用户无法修正可能的识别偏差（优先级：紧急）。
- **中**: **元数据手动覆盖**。需要增加“删除单项元数据”及手动触发特定维度提取的功能（优先级：正常）。
- **低**: **文献搜索过滤**。随着文档增多，需要增加基于标题和作者的本地过滤积木（优先级：优化）。

## 4. 设计规范参考
- **颜色 Token**: 
    - `#10b981` (Complete - Emerald-500)
    - `#f59e0b` (Partial - Amber-500)
    - `#f43f5e` (Failed - Rose-500)
- **边框**: 统一使用 `border-2` 加圆角 `rounded-3xl`。
- **动效**: 使用 Framer Motion 的 `AnimatePresence` 实现任务队列的平滑弹出与收起。

## 5. 操作指引
- **如何继续**: 
    1.  运行 `.\run_tests.bat` 确认 12 个测试点全绿。
    2.  若需增加新功能，先在 `src/types/index.ts` 定义数据模型。
- **如何回滚**: `git reset --hard HEAD~1` (每个 commit 均通过全量测试，可放心回退)。
- **项目启动**: `.\run_app.bat`。
- **注意事项**: 
    - **配置**: 直接改 `backend/config.py` 即可生效。
    - **测试**: 严禁绕过 `run_tests.bat` 直接 Commit。
