# Handoff: Academic Clarity - 工业级高并发与动态交互版 (2026-04-21)

## 1. 项目概述
- **项目名称**: Academic Clarity (AI Researcher)
- **类型**: 桌面级学术文献管理与 AI 增强阅读器。
- **技术栈**: 
    - **前端**: React 19 + Vite (Port: 30517) + Framer Motion + react-pdf。
    - **后端**: Python 3 + FastAPI (Port: 38392) + SQLite (WAL 模式)。
    - **核心引擎**: 10-Worker 并行任务中枢 (`asyncio.Queue`)。
- **前端包管理器**: pnpm (v10+)。
- **前端目录**: `src/` (组件化架构)。
- **一句话说明**: 这是一个支持 10 路 AI 并发处理（OCR + 提取）的学术智库，具备全维度手动拖拽布局和结果导向的自动同步能力。

## 2. 本次会话完成的工作
- **目标**: 实现高性能任务调度、全维度伸缩 UI 以及高保真 OCR 闭环，确保 100% 验收测试通过。

### Git 节点记录
| Hash | 描述 | 关键改动 |
| :--- | :--- | :--- |
| `b9f616d` | feat: stabilize industrial OCR... | 锁定 4000 Token 限制，引入 Blob 加载 PDF，对齐跨栈测试。 |
| `e8f0129` | test: complement UI tests... | 补全筛选、拖拽及后端 JOIN 的 100% 行为测试。 |
| `ef9055d` | feat: implement manual resizing... | 手写高性能拖拽 Hook，引入 react-pdf 实现 Fit-to-Width。 |
| `3c55850` | feat: implement intelligence filtering... | 后端 SQL 聚合查询，前端实现按期刊、年份的智慧筛选。 |
| `e5e63eb` | feat: merge official OCR cleanup... | 整合官方正则清洗逻辑，彻底剔除布局标签保护正文。 |
| `957570c` | feat: implement 10-concurrency TaskHub... | 建立全局异步队列 Worker 池，实现失败自动队尾重试。 |
| `cb514cc` | feat: simplify Dashboard UI... | 移除选项卡，确立红/黄/绿情报完整度视觉体系。 |
| `2f37745` | feat: result-driven auto-sync... | 切换目录即刻触发缺失情报文档的自动补全。 |
| `4c277c1` | feat: implement config fallback... | 建立本地 `config.py` 机制，支持私有 Key 隔离与分发。 |

### 具体改动清单
1.  **多轴手动伸缩布局 (Resizable Layout)**
    - **文件**: `src/components/Reader.tsx`, `src/hooks/useResizable.ts`
    - **改动**: 弃用第三方库，手写 `useResizable` Hook 管理 `leftWidth`, `rightWidth` 和 `splitRatio`。
    - **决策**: 解决第三方库类型冲突问题，同时在 PDF/Markdown 分屏区实现百分比比例拖拽。
2.  **情报导向筛选系统 (Intelligence Filtering)**
    - **文件**: `backend/database.py`, `src/components/Dashboard.tsx`
    - **改动**: 后端 `get_all_documents` 增加 `LEFT JOIN` 子查询聚合 `Basic Insight`。前端增加 `searchQuery`, `selectedJournal`, `selectedYear` 过滤状态。
    - **决策**: 将过滤压力放在前端 `useMemo`，后端一次性吐出聚合数据，减少 API 往返。
3.  **工业级 OCR 流水线 (Token-Balanced Pipeline)**
    - **文件**: `backend/services/ai_service.py`, `backend/services/ocr_service.py`
    - **改动**: 将 OCR 的 `max_tokens` 限制在 4000（防止撞 8192 总序列墙）。增加 DOI 专项 Prompt 引导。
    - **决策**: 破解了 DOI 在页面底部被截断的谜团，确保页脚元数据 100% 捕获。
4.  **Blob 安全加载引擎 (PDF Blob Loading)**
    - **文件**: `src/components/reader/PdfViewer.tsx`, `backend/server.py`
    - **改动**: 前端使用 `fetch` 抓取流并转换为 `URL.createObjectURL(blob)`。后端增加 `urllib.parse.unquote` 处理特殊文件名。
    - **决策**: 彻底解决文件名带空格、特殊符号导致的“Failed to load PDF”报错。

### 已修改文件列表
| 文件路径 | 改动摘要 |
| :--- | :--- |
| `src/tests/setup.ts` | **环境 Mock**: 补齐了 `DOMMatrix` 和 `ResizeObserver`，支持 PDF 渲染测试。 |
| `backend/core/task_manager.py` | **并发中枢**: 核心 TaskHub 类，管理 10 个常驻异步 Worker。 |
| `backend/utils/text_processor.py` | **高保真清洗**: 官方级正则逻辑，移除 `<|ref|>` 但保护 DOI 所在的行。 |
| `src/types/index.ts` | **类型中心**: `Document` 接口新增 `basic_insight_json` 字段。 |

## 3. 尚未完成的工作
- **高**: **OCR Markdown 编辑器**。目前内容只读，用户发现错别字无法手动校正（优先级：极高）。
- **中**: **批量操作界面**。目前只能一键同步全库，缺乏“选中多项删除/重新处理”的 UI（优先级：中）。
- **低**: **全局元数据导出**。需要一键生成整个工作区的 Excel 或 BibTeX 报告（优先级：低）。

## 4. 设计规范参考
- **颜色系统**: 
    - 🔴 `#f43f5e` (Rose-500) -> 任务失败或无原文。
    - 🟡 `#f59e0b` (Amber-500) -> 仅有原文，缺 AI 洞察。
    - 🟢 `#10b981` (Emerald-500) -> 情报闭环，全绿。
- **拖拽手柄**: 1.5px 宽的 `slate-200` 线条，Hover 时变为 `indigo-400` 并显示 `GripVertical` 图标。

## 5. 操作指引
- **如何继续**: 
    1.  运行 `.\run_tests.bat`。必须看到 12 个任务全绿。
    2.  添加新字段时，必须先改 `backend/database.py` 的聚合 SQL。
- **如何回滚**: `git reset --hard b9f616d` (此节点为今日最稳健版本)。
- **项目启动**: `.\run_app.bat`。
- **注意事项**: 
    - **本地 Key**: 修改 `backend/config.py`，不要动 `config.py.example`。
    - **测试**: 任何 UI 按钮修改必须更新对应的 `data-testid`。
