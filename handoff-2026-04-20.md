# Handoff: Academic Clarity 项目交接文档 (2026-04-20)

## 1. 项目概述
- **项目名称**: Academic Clarity (AI Researcher)
- **项目类型**: 桌面级学术文献管理与 AI 增强阅读器。
- **技术栈**: 
    - **前端**: React 19 + TypeScript + Vite + Tailwind CSS + Framer Motion (积木化组件)。
    - **外壳**: Electron 41 (管理主进程与 Python 后端生命周期)。
    - **后端**: Python 3 (FastAPI/Uvicorn) + SQLite (WAL 模式)。
    - **AI 引擎**: SiliconFlow (DeepSeek-OCR) + 本地 LLM (localhost:37210 / gpt-4.1)。
- **包管理器**: pnpm (v10+)。
- **目录结构**: 前端源码在 `src/`，后端业务逻辑在 `backend/`，测试套件在 `tests/`。
- **一句话说明**: 这是一个基于“物理隔离工作区”理念的学术智库，支持 PDF 自动同步、高精度 OCR、多维结构化元数据提取及 AI 沉浸式对话。

## 2. 本次会话完成的工作
- **目标**: 将项目从单一功能的 PDF 查看器原型，重构为**模块化、高安全性、具备全自动同步流水线与 100% 测试覆盖率**的生产级应用。

### Git 节点记录
| Hash | 描述 | 关键改动 |
| :--- | :--- | :--- |
| `5a6282b` | docs: codify categorized testing... | 在 `GEMINI.md` 中制度化分层测试与路径自适应规范。 |
| `ccf1ebf` | chore: categorize tests... | 完成测试文件物理分类，实现递归式 `PYTHONPATH` 溯源。 |
| `97396ea` | feat: implement unified type system... | 建立 `src/types/index.ts`，实现全栈类型对齐。 |
| `b4d77d8` | feat: implement exhaustive retry logic... | 引入 OCR 指数退避重试积木与 API 级上传验证。 |
| `e18fd81` | feat: complete autonomous workspace... | 实现零点击 PDF 自动入库与全自动 OCR 处理流。 |
| `5b57fbf` | feat: implement self-contained workspace... | 建立 `document_metadata` 多维表，支持一文多 JSON。 |

### 具体改动清单
1.  **多维元数据积木 (Metadata Insight)**
    - **文件**: `backend/database.py`, `src/components/reader/MetadataPanel.tsx`
    - **改动**: 新增 `document_metadata` 表及 `MetadataNode` 递归渲染组件。
    - **决策**: 采用递归渲染以适配 LLM 生成的不确定深度 JSON；通过 Label 标识不同维度的科研视角（如 "Basic Insight", "Methodology"）。
2.  **全自动同步流 (Auto-Sync Pipeline)**
    - **文件**: `backend/services/workspace_service.py`, `backend/server.py`
    - **改动**: 建立 `WorkspaceService`，在后端启动或切换路径时自动调用 `scan_and_sync`。
    - **决策**: 实现“文件夹即数据库”，新文件物理存入后无需手动上传，自动触发 OCR -> 提取基础信息。
3.  **安全性加固 (Security Hardening)**
    - **文件**: `electron/preload.ts`, `backend/utils/security.py`
    - **改动**: 移除 `ipcRenderer` 全量暴露，改为具名 API；对上传的文件名执行 `secure_filename` 脱敏。
    - **决策**: 彻底防御 XSS 提权和路径穿越漏洞。
4.  **超并行质量防线 (QA Engine)**
    - **文件**: `tests/run_all_tests.py`, `run_tests.bat`
    - **改动**: 整合 `tsc`, `Vitest` 和 `unittest` 进同一个并行进程池。
    - **决策**: 将前端 Lint 作为并行任务之一，大幅缩短验收耗时（平均 ~40s 跑通 11 个任务）。

### 已修改文件列表
| 文件路径 | 改动摘要 |
| :--- | :--- |
| `src/api/client.ts` | **核心积木**: 统一管理所有 API 调用，遵循 DRY 原则。 |
| `backend/server.py` | **胶水层**: 移除业务逻辑，仅保留 FastAPI 路由和 Service 调用。 |
| `src/types/index.ts` | **类型中心**: 全局共享的 Interface 事实来源。 |
| `GEMINI.md` | **项目宪法**: 规定了 100% UI 测试、WAL 模式和测试分类规范。 |
| `run_app.bat` | **一键启动**: 优化为无感退出，支持命令行直接调用。 |

## 3. 尚未完成的工作
- **高**: **OCR 结果的在线编辑**。目前 Markdown 是只读的，学术场景下可能需要用户手动修正 OCR 幻觉。
- **中**: **跨文档全局搜索**。目前只能针对单篇文档聊天，尚未实现基于 RAG 的全局知识检索。
- **低**: **一键导出 BibTeX**。虽然有了元数据 JSON，但还没做格式化转换工具。

## 4. 设计规范参考
- **主色调**: `Indigo-600` (#4f46e5) 作为强调色，`Slate-50/100` 作为背景，营造严谨且现代的实验室氛围。
- **圆角规范**: 统一使用 `rounded-3xl` (24px) 和 `rounded-xl` (12px)，通过大圆角减弱学术软件的枯燥感，增加商业亲和力。
- **交互 Token**: 
    - Hover 态：`y: -4`, `box-shadow: 25px (soft)`。
    - 状态色：`emerald` (Success), `amber` (Process), `rose` (Fail)。

## 5. 操作指引
### 如何继续
1.  **自测**: 修改代码后必须运行 `.\run_tests.bat`。如果通过，说明没有破坏现有的“积木”结构。
2.  **新增功能**: 请在 `src/api/client.ts` 中声明新接口，在 `src/types/index.ts` 中声明新类型。
3.  **UI 交互**: 每一个新按钮必须在 `tests/` 对应的 `.test.tsx` 中添加点击验证。

### 关键命令
- **启动**: `.\run_app.bat`
- **质量验收**: `.\run_tests.bat` (包含 Lint + Vitest + Python)
- **回滚**: `git reset --hard HEAD~1` (如遇严重回归)

### 注意事项
- **严禁直接修改 `dist/` 或 `node_modules/`**。
- **Electron 版本过高**，如遇原生库编译失败（better-sqlite3），请优先考虑将逻辑移至 Python 后端处理，保持前端纯净。
- **不要添加 `pause` 命令**，用户偏好静默的命令行流。
